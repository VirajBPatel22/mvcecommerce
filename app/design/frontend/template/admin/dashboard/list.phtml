<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Dashboard</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"/>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
  <style>
    .dashboard-card {
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-radius: 12px;
      padding: 20px;
      color: #fff;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .dashboard-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0,0,0,0.2);
    }
    .card-icon {
      font-size: 3rem;
      opacity: 0.8;
    }
    .card-title {
      font-weight: 600;
      margin-bottom: 10px;
    }
    .card h2 {
      font-weight: 700;
    }
  </style>
</head>
<body>
<a class = "add-new" href="<?php echo $this->getUrl('*/*/exportCsv'); ?>">Export to CSV</a>
  <div class="container mt-5">
    <h2 class="text-center mb-4">Admin Dashboard</h2>
    <div class="row g-4">
      <!-- Orders Count -->
      <div class="col-md-3">
        <div class="dashboard-card bg-primary">
          <div>
            <h5 class="card-title">Orders</h5>
            <h2><?php echo $this->getTotalOrder()['total_order']?></h2>
          </div>
          <i class="fas fa-shopping-cart card-icon"></i>
        </div>
      </div>

      <!-- Products Count -->
      <div class="col-md-3">
        <div class="dashboard-card bg-success">
          <div>
            <h5 class="card-title">Products</h5>
            <h2><?php echo $this->getTotalProduct()['total_product'];?></h2>
          </div>
          <i class="fas fa-boxes card-icon"></i>
        </div>
      </div>

      <!-- Categories Count -->
      <div class="col-md-3">
        <div class="dashboard-card bg-warning">
          <div>
            <h5 class="card-title">Categories</h5>
            <h2><?php echo $this->getTotalCategory()['total_category']?></h2>
          </div>
          <i class="fas fa-list card-icon"></i>
        </div>
      </div>

      <!-- Customers Count -->
      <div class="col-md-3">
        <div class="dashboard-card bg-danger">
          <div>
            <h5 class="card-title">Customers</h5>
            <h2>250</h2>
          </div>
          <i class="fas fa-users card-icon"></i>
        </div>
      </div>
    </div>
  </div>

</body>
</html>
